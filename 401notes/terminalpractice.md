## Command Line

The command line is a text based interface to the system in which we are able to enter commands by typing them on the keyboard an feedback will be given to you as text.

## Basic Navigation

PWD stands for print working directory, which tells what directory you are currently in. Absolute paths specify a location and begin with a forward slash.Relative specify a location relative to the current working directory and do not begin with a forward slash. Manual pages are a set of pages that explain every command available on your system.

## File Manipulation

Mkdir is how you create a directory. Rmdir is how you remove a directory. Mv moves a directory. 

## Cheat Sheet

The cheat sheet is very informative. It has every command listed as a quick way to remember the commands at a glance.
